const nostr = requie('nostr-tools');
const require = require('relay');
const kind = require('kind0');

const relay = {
  relay: wss://relay.damus.io;
  relay: wss://arealayer.relay.com;

const npub

const nsec

